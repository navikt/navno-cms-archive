FROM node:22-bookworm-slim

RUN apt-get update \
    && apt-get --assume-yes --no-install-recommends install chromium \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app/xp-archive/server

ENV NODE_ENV=production
ENV PUPPETEER_SKIP_DOWNLOAD=true
ENV PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
ENV XDG_CONFIG_HOME=/tmp/.chromium
ENV XDG_CACHE_HOME=/tmp/.chromium

RUN mkdir -p /app/xp-archive/server /tmp/.chromium \
    && chown -R node:node /app /tmp/.chromium

COPY --chown=root:root --chmod=755 node_modules /app/node_modules/
COPY --chown=root:root --chmod=755 xp-archive/.env /app/xp-archive/
COPY --chown=root:root --chmod=755 xp-archive/node_modules* /app/xp-archive/node_modules/
COPY --chown=root:root --chmod=755 xp-archive/server/dist /app/xp-archive/server/dist/
COPY --chown=root:root --chmod=755 xp-archive/server/package.json /app/xp-archive/server/

USER node

EXPOSE 3499
CMD ["npm", "run", "start"]
