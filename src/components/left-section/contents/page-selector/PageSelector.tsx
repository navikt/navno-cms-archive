import React from 'react';
import { CmsContentDocument } from '../../../../../common/cms-documents/content';

type Props = {
    contents: CmsContentDocument[];
};

export const PageSelector = ({ contents }: Props) => {
    return <div>{'Hello'}</div>;
};
